# URL del API Gateway (backend)
VITE_API_GATEWAY_URL=http://localhost:8080

# Usar mock de autenticación (solo desarrollo sin backend)
# Cambiar a false cuando el backend esté corriendo
VITE_USE_MOCK_AUTH=false
